apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: sharedservices.areogear.org
spec:
  group: areogear.org
  names:
    kind: SharedService
    listKind: SharedServiceList
    plural: sharedservices
    singular: sharedservice
  scope: Namespaced
  version: v1alpha1
  validation:
    openAPIV3Schema:
      properties: