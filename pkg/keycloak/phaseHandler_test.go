package keycloak

import "testing"

func TestPhaseHandler_Initialise(t *testing.T) {
	cases := []struct {
		Name string
	}{
		{
			Name: "test expected state after initialise",
		},
	}

	for _, tc := range cases {
		t.Run(tc.Name, func(t *testing.T) {

		})
	}
}
